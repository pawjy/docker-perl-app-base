machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t quay.io/wakaba/docker-perl-app-base .
    - docker build -t quay.io/wakaba/docker-perl-app-base:sid sid/
test:
  override:
    - docker run quay.io/wakaba/docker-perl-app-base /app/perl -v
    - docker run quay.io/wakaba/docker-perl-app-base:sid /app/perl -v

    - docker run quay.io/wakaba/docker-perl-app-base bash -c 'git clone https://github.com/manakai/perl-web-resource /res && mv /res/.git /app/ && cd /app && make test-real'
    - docker run quay.io/wakaba/docker-perl-app-base:sid bash -c 'git clone https://github.com/manakai/perl-web-resource /res && mv /res/.git /app/ && cd /app && make test-real'

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS quay.io
      - docker push quay.io/wakaba/docker-perl-app-base
      - docker push quay.io/wakaba/docker-perl-app-base:sid
