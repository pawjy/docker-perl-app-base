{
  "circleci": {
    "docker-build": {
      "name": "quay.io/wakaba/docker-perl-app-base:sid",
      "path": "sid/",
      "branch": "sid"
    },
    "tests": [
      "docker run quay.io/wakaba/docker-perl-app-base:sid /app/perl -v",
      "docker run quay.io/wakaba/docker-perl-app-base:sid bash -c 'git clone https://github.com/manakai/perl-web-resource /res && mv /res/.git /app/ && cd /app && git reset --hard && make test-real'"
    ]
  }
}
